describe('Data type', () => {
  it('should must declare', () => {
    const name: string = 'dhimas sahid pangestu'; // stri
    const balance: number = 1000000000;
    const isVip: boolean = true;
  });
});
